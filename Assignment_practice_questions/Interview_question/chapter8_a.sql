1) What is a subquery?
A subquery is a query written inside another SQL query.

2) What is the difference between a simple subquery and a correlated subquery?
A simple subquery runs once independently, while a correlated subquery runs for each row of the outer query.